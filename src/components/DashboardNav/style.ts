import { styled } from "styled-components";

export const StyledNav = styled.nav`
    width: 100%;

    ul {
        width: 100%;
        display: flex;
        flex-direction: row;
        flex-wrap: wrap;

        gap: 2.5rem;
    }
`